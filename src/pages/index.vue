<script setup lang="ts">
import { useFetchRestaurants } from '~/composables/restaurants';

const { data: restaurants, isError } = useFetchRestaurants();
</script>

<template>
  <div>
    <RatingFilter />
    <LoadingError v-if="isError" />
    <div v-else-if="restaurants" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 my-4">
      <RestaurantCard
        v-for="restaurant of restaurants"
        :key="restaurant.id"
        :restaurant="restaurant"
      />
    </div>
  </div>
</template>
